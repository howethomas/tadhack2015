= simple_form_for @signer, :html => { :class => 'form-horizontal' } do |f|
|   = f.input :mobile
|   = f.input :name
|   = f.input :town
|   = f.input :signed
| |   .form-actions
    = f.button :submit, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), signers_path, :class => 'btn'
| 